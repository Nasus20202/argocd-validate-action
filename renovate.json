{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^action\\.yaml$/"
      ],
      "matchStrings": [
        "uses: azure/setup-kubectl@[^\\s]+\\s+with:\\s+version: \"(?<currentValue>v.+?)\""
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "kubernetes/kubernetes",
      "versioningTemplate": "loose"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^action\\.yaml$/"
      ],
      "matchStrings": [
        "uses: azure/setup-helm@[^\\s]+\\s+with:\\s+version: \"(?<currentValue>v.+?)\""
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "helm/helm",
      "versioningTemplate": "loose"
    }
  ]
}
